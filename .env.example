DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=fragrance
DB_PASSWORD=
# Keep the existing database when upgrading.
DB_NAME=scent_craft_db
PORT=3000
# Generate a random secret with at least 32 characters. Do not commit the real value.
SESSION_SECRET=
# Set production only behind HTTPS (session cookies then require Secure).
NODE_ENV=development
